# SPEC.md — senzai-niwa(潜在の庭)

<!-- scaffold template v1.24.0 から展開(2026-09-02)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

MNIST で学習した VAE(潜在 2 次元)の潜在平面を、マウス/指で「歩く」と
手書き数字がその場で生まれる生成アプリ。tegaki-yomi(読む)の逆向きの姉妹作(産む)。
学習はフリート初の PyTorch(手元専用・training/)。ブラウザには丸め済みデコーダ重みと
潜在地図データだけを載せ、生成は TypeScript 手書きデコーダで行う。
正しさの正本は**二実装照合オラクル** — 丸め済み重みを読み戻した Python(numpy・float64)側の
出力が正、TS 側が写し。ブラウザ完結・静的エクスポート・課金経路ゼロ。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 学習パイプライン(`training/train_vae.py`・PyTorch): MNIST で VAE を学習し、(a) 丸め済みデコーダ重み JSON、(b) 照合フィクスチャ(丸め済み重みを numpy float64 で読み戻して再計算した潜在点 64 件のピクセル値)、(c) 潜在地図(テスト 10,000 件のエンコーダ μ とラベル)を出力する。シード固定 | must |
| F-02 | モデル: encoder 784→512→256→(μ, logσ²)(潜在 2)、decoder 2→256→512→784(隠れ ReLU・出力 sigmoid)。損失は BCE + KL | must |
| F-03 | TS デコーダ(純関数): 丸め済み重み JSON を読み、decode(z: [x,y]) → 784 ピクセル([0,1])を実装 | must |
| F-04 | 潜在平面 UI: 平面([-3,3]²)をポインタ/タッチでなぞると 28×28 の生成画像が即時更新される | must |
| F-05 | 潜在地図: テスト集合の μ 散布(0〜9 のクラス別配色)を平面の背景に敷き、「どこを歩けば何が生まれるか」を見せる | must |
| F-06 | モーフ: 現在位置を始点/終点として記憶し、再生で 2 点間を往復補間(コサイン緩急)しながら連続生成。ドラッグ開始で停止。補間 `morphZ(a,b,u)` は純関数(u∈[0,1] に clamp・非有限値は TypeError) | must(loop_003) |
| F-07 | 格子俯瞰: 潜在平面を 15×15 格子でデコードした一覧図を開閉できる。格子の潜在点列 `gridLatents(n, range)` は純関数(行優先・左上が (-r, +r))。一覧図のセルを押すとその座標へ移動 | must(loop_003) |
| F-08 | フッタ: MIT License・GitHub・操作説明・設計図・App Menu の 5 リンク(フリート共通規約) | must(公開ループ) |
| F-09 | 庭の切替: 「数字の庭(MNIST)」と「くずし字の庭(KMNIST)」をタブで切り替える。資産は庭ごとに `src/core/model/<garden>/` に分け、動的 import で必要な庭だけ読み込む。切替時は z・モーフ・格子を初期化。凡例(クラス名 × 配色)を平面の下に出す — KMNIST のクラス名はデータ配布元の classmap(お・き・す・つ・な・は・ま・や・れ・を)から導く | must(loop_004) |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的エクスポート(`output: "export"`)。生成は全てブラウザ内・外部通信なし | `next build` が out/ を生成 |
| N-02 | `src/core` は純関数のみ。カバレッジ lines/functions/statements ≥ 90%, branches ≥ 85%(重み等のデータ資産は対象外) | eslint 境界規則 + vitest coverage 閾値 |
| N-03 | 生成はポインタイベント終端で毎回実行(decoder forward は約 0.5M FLOP。実測 1.19 ms — §4)。Canvas API は effect / ハンドラ内のみ | HC-002 予防適用 |
| N-04 | PyTorch は手元専用。Vercel へ渡るのは静的成果物(JSON)のみ。cron ゼロ・関数ゼロ | リポジトリ構成 + out/ の内容 |
| N-05 | 縁(潜在平面の範囲外座標・非有限値入力)は正常系として仕様化 | core テスト |

## 4. 品質基準

`npm run verify` green を全ループの完了条件とする(内訳は AGENTS.md §3)。

二実装照合ゲート(丸め済み重みを読み戻した numpy float64 が正、TS が写し)。
**両方の庭(mnist / kmnist)それぞれに適用する**(loop_004 で拡張):

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | ピクセル照合 | フィクスチャ 64 潜在点で TS decode の 784 値が Python 再計算値と最大絶対誤差 < 1e-9 で一致 |
| G-02 | 形状・決定論 | 重み JSON の形状(2×256, 256, 256×512, 512, 512×784, 784)を検査。同一 z → 深い等値 |
| G-03 | 再構成の質 | Python 側でテスト 10,000 件の再構成 BCE(丸め済み重み・読み戻し後)を assets のメタに記録し、テストはメタ値が庭ごとの較正済み閾値以下であることを検査する。mnist: < 0.18(較正: 30 エポック 0.16716・対照 1 エポック 0.19998 — 2026-09-02)。kmnist: 閾値は loop_004 で 1 エポック対照 → 本学習の順に実測して置く |
| G-04 | 潜在地図の整合 | 潜在地図 JSON はテスト全件(counts はデータから導出)・ラベル 0〜9・座標が有限値。クラス名 10 件は配布元 classmap 由来(kmnist)/数字(mnist) |

loop_001(L0 較正)の実測記録欄:

- テスト 10,000 件の再構成 BCE(/pixel): 0.16716(2026-09-02・30 エポック・丸め済み重み読み戻し。対照: 1 エポックは 0.19998)
- デコーダ重み JSON のサイズ: 5,489 KB / gzip 1,821 KB。latent_map.json 193 KB / gzip 57 KB(2026-09-02)
- TS decode 1 回の所要時間: 1.19 ms(2026-09-02・vitest 内 100 回平均・timing.test.ts)— ms 級で N-03 成立

loop_002(公開)の実測記録欄:

- 本番: https://senzai-niwa.vercel.app(2026-09-03 初回デプロイ・CLI 運用・Git 連携なし)
- 本番検品: フッタ規約 3 幅(1100/640/375px)+ 対話(ドラッグ→生成・コンソールエラー 0)で合格(2026-09-03)
- 解説: 歩き方 7b166775-0ae0-481f-8552-af41a0f4e15b / 設計図 057fb018-6586-4bbf-a014-f2d541a2581f
- app-menu: cat12「実データ分析」に掲載・本番反映済み(92 枚・2026-09-03)

loop_003(F-06/F-07)の実測記録欄:

- 本番反映: 2026-09-03(モーフ再生・格子俯瞰とも本番検品で動作確認 — モーフ中の z が
  A→B へ実際に動くこと・格子 225 セルの平均輝度 31.3・セル押下で移動、を実測)
- 検品器の教訓 2 件: 掴み置きした boundingBox は自動スクロールでずれる(VERIF-FALSE →
  HC-080 どおり毎回引き直す)・横溢れ 0 は横並びの保証ではない(GEN-LOGIC ×2 → HC-142 起票、
  検品器に「1100px で plane と出力パネルが同じ帯」の表明を追加)
- デプロイ枠: 二日連続で日次上限に当たり HC-144 起票(再試行モニタ 15 分間隔で自動取得)

loop_004(F-09 くずし字の庭)の実測記録欄:

- KMNIST(CODH・CC BY-SA 4.0)VAE: テスト 10,000 字の再構成 BCE/pixel 0.33271
  (2026-09-03・30 エポック・丸め済み重み読み戻し。対照: 1 エポックは 0.36208 — G-03 閾値 0.345 の較正元)
- MNIST 再生成(classNames 追加)は BCE 0.16716 で前回と完全一致(2026-09-03・再現性の傍証)
- 資産: kmnist weights 5,457 KB / fixtures 1,048 KB / latent_map 192 KB。庭ごとに別チャンク
  (約 5MB × 2)へ分割され、必要なほうだけ動的読み込みされることをビルド出力で確認(2026-09-03)
- 本番反映: 2026-09-03(庭切替・凡例 お〜を・潜在地図・生成とも本番検品で合格)

## 5. スコープ外

- ブラウザ内での学習・再学習(学習は手元の PyTorch のみ)
- ONNX Runtime 等の推論ランタイム導入(デコーダは TS 手書き。二実装照合を成立させるため)
- KMNIST 版の庭(将来ループ候補。kuzushi-yomi との姉妹関係で拡張しうる)
- 条件付き生成(クラス指定生成)・拡散モデル・GAN
- 描いた絵のエンコード(エンコーダはブラウザに載せない。潜在地図の事前計算にのみ使う)
